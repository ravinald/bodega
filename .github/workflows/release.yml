name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write       # create releases
  id-token: write       # cosign keyless signing via OIDC
  attestations: write   # SLSA provenance via actions/attest-build-provenance

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0    # GoReleaser needs full history for changelog
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          check-latest: true

      - uses: anchore/sbom-action/download-syft@v0
      - uses: sigstore/cosign-installer@v3

      - uses: goreleaser/goreleaser-action@v7
        with:
          # This job signs and attests what people install, so the tool that
          # builds it is a version somebody chose rather than whatever shipped
          # the morning of the tag.
          version: v2.18.2
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Attest binaries
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: 'dist/bodega_*'
